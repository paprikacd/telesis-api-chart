apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "telesis-api.fullname" . }}
  labels:
    {{- include "telesis-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env.nonSecret }}
  {{ $key }}: {{ $value | toString | quote }}
  {{- end }}

